doctype html
html(lang='pt-br')
    include header.pug

    body
        // Header
        header
            .wrapper
                h1.sprite Rapiddo

        // Main
        section#survey
            .wrapper
                h3 Onde você prefere ganhar os 10% de volta?
                p.legend Selecione uma ou mais opções.

                .error
                    p Você precisa selecionar no mínimo uma opção abaixo, antes de continuar.

                form(id='form-prefs', method='POST')
                    // Mercado
                    input(type='checkbox', id='pick-market', name='Mercado', value='Mercado')
                    label(for='pick-market')
                        .inputs
                            span.sprite.icon-market
                            | Compras no Mercado

                    // Comida
                    input(type='checkbox', id='pick-food', name='Comida', value='Comida')
                    label(for='pick-food')
                        .inputs
                            span.sprite.icon-food
                            | Entrega de Comida

                    // Música
                    input(type='checkbox', id='pick-music', name='Música', value='Música')
                    label(for='pick-music')
                        .inputs
                            span.sprite.icon-music
                            | Músicas

                    // News
                    input(type='checkbox', id='pick-news', name='Conteudo', value='Conteudo')
                    label(for='pick-news')
                        .inputs
                            span.sprite.icon-news
                            | Jornais e Revistas

                    // Recarga
                    input(type='checkbox', id='pick-recharge', name='Recargas', value='Recargas')
                    label(for='pick-recharge')
                        .inputs
                            span.sprite.icon-recharge
                            | Recargas

                    // Tickets
                    input(type='checkbox', id='pick-tickets', name='Ingressos', value='Ingressos')
                    label(for='pick-tickets')
                        .inputs
                            span.sprite.icon-tickets
                            | Ingressos

                    // Contas
                    input(type='checkbox', id='pick-bills', name='Contas', value='Contas')
                    label(for='pick-bills')
                        .inputs
                            span.sprite.icon-bills
                            | Contas (luz, água, etc)

                    // Kids
                    input(type='checkbox', id='pick-kids', name='Desenhos', value='Desenhos')
                    label(for='pick-kids')
                        .inputs
                            span.sprite.icon-kids
                            | Desenhos Infantis

                    // Entrega
                    input(type='checkbox', id='pick-delivery', name='Entrega', value='Entrega')
                    label(for='pick-delivery')
                        .inputs
                            span.sprite.icon-delivery
                            | Entrega de Documentos



        // Footer
        footer
            a(href='#', onclick="ga('send', 'event', '[btn] Escolher e continuar', 'click', '2o Funil');").cta-button Escolher e continuar

        script(src='resources/js/jquery.js', type='text/javascript')
        script(type='text/javascript').
            $(".cta-button").on( "click", function() {
                var inputs = $("input:checked");

                if (inputs.length > 0) {
                    for (i = 0; i < inputs.length; i++) {
                        ga('send', 'event', '[picked]', inputs[i].name);
                    }
                    document.location = "compartilhar.html";
                    return false;

                } else {
                    $('.error').fadeIn('slow').delay(3000).fadeOut('slow');

                }

            });

        script(type='text/javascript').
            (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
            })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
            ga('create', 'UA-93185721-9', 'auto');
            ga('send', 'pageview');
